import {test,expect} from "@playwright/test";test("login to AI search to appointment list",async({page})=>{await page.goto("/login");await page.getByLabel("Email address").fill("admin@medibook.demo");await page.getByLabel("Password").fill("Demo@123");await page.getByRole("button",{name:/sign in/i}).click();await expect(page).toHaveURL(/overview/);await page.getByRole("link",{name:"AI Assistant"}).click();await page.getByPlaceholder(/Ask about appointments/).fill("Show available cardiologists for tomorrow.");await page.getByLabel("Send").click();await expect(page.getByText(/cardiolog|available/i).last()).toBeVisible();await page.getByRole("link",{name:"Appointments"}).click();await expect(page.getByText(/APT-/).first()).toBeVisible()});
