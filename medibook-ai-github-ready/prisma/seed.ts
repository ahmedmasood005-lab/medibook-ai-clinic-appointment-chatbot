import { promises as fs } from "node:fs";import path from "node:path";async function main(){const target=path.join(process.cwd(),"data","medibook.json");await fs.rm(target,{force:true});console.log("Seed reset complete. Start the app once to generate 4 roles/users, 60 patients, 12 doctors, 8 departments and 220 fictional appointments.")}main().catch(e=>{console.error(e);process.exit(1)});
