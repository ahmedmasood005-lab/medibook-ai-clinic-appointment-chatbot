import {requireUser} from "@/lib/auth";import {readStore} from "@/lib/store";import {PageHeader} from "@/components/page";export default async function Users(){await requireUser(["Administrator"]);const d=await readStore();return <div className="page-pad" style={{padding:26}}><PageHeader title="User management" description="Administrator-only role and account visibility."/><div className="card table-wrap"><table className="table"><thead><tr><th>User</th><th>Email</th><th>Role</th><th>Status</th><th>Created</th></tr></thead><tbody>{d.users.map(u=><tr key={u.id}><td><strong>{u.name}</strong></td><td>{u.email}</td><td><span className="badge" style={{background:"#e8f0ff",color:"#1d4ed8"}}>{u.role}</span></td><td><span className={`badge status-${u.active?"Confirmed":"Cancelled"}`}>{u.active?"Active":"Inactive"}</span></td><td>Fictional demo record</td></tr>)}</tbody></table></div></div>}
