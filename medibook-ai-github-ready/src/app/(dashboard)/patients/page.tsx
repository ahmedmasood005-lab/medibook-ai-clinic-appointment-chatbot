import {createPatientAction} from "@/app/actions";import {requireUser} from "@/lib/auth";import {readStore} from "@/lib/store";import {PageHeader} from "@/components/page";export default async function Patients(){const [u,d]=await Promise.all([requireUser(),readStore()]);const canEdit=["Administrator","Receptionist"].includes(u.role);return <div className="page-pad" style={{padding:26}}><PageHeader title="Patients" description="Minimal patient registration and appointment history. No clinical records are displayed."/>{canEdit&&<details className="card" style={{padding:16,marginBottom:14}}><summary style={{fontWeight:800,cursor:"pointer"}}>Add patient</summary><form action={createPatientAction} style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:12,marginTop:15}}><input className="field" name="name" placeholder="Full name" required/><input className="field" name="email" type="email" placeholder="Email" required/><input className="field" name="phone" placeholder="Phone" required/><select name="gender" className="field"><option value="">Gender (optional)</option><option>Female</option><option>Male</option><option>Other</option></select><button className="btn btn-primary">Register patient</button></form></details>}<div className="card table-wrap"><table className="table"><thead><tr><th>Patient</th><th>Contact</th><th>Registered</th><th>Total appointments</th><th>Upcoming</th></tr></thead><tbody>{d.patients.map(p=>{const ap=d.appointments.filter(a=>a.patientId===p.id);return <tr key={p.id}><td><strong>{p.name}</strong><br/><small className="muted">{p.gender||"Not specified"}</small></td><td>{p.email}<br/><small className="muted">{p.phone}</small></td><td>{new Date(p.createdAt).toLocaleDateString()}</td><td>{ap.length}</td><td>{ap.filter(a=>new Date(a.startsAt)>new Date()&&a.status!=="Cancelled").length}</td></tr>})}</tbody></table></div></div>}
