import {Clock3,Stethoscope} from "lucide-react";import {readStore} from "@/lib/store";import {PageHeader} from "@/components/page";export default async function Doctors(){const d=await readStore();return <div className="page-pad" style={{padding:26}}><PageHeader title="Doctors" description="Professional profiles, working schedules, consultation fees, and utilization."/><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",gap:14}}>{d.doctors.map(x=>{const ap=d.appointments.filter(a=>a.doctorId===x.id);const completed=ap.filter(a=>a.status==="Completed").length;return <article className="card" key={x.id} style={{padding:18}}><div style={{display:"flex",gap:12,alignItems:"center"}}><span style={{width:44,height:44,borderRadius:10,background:"#e8f0ff",display:"grid",placeItems:"center",color:"var(--blue)"}}><Stethoscope size={22}/></span><div><h2 style={{fontSize:16,margin:0}}>{x.name}</h2><small className="muted">{x.title} · {x.specialization}</small></div><span className={`badge status-${x.active?"Confirmed":"Cancelled"}`} style={{marginLeft:"auto"}}>{x.active?"Active":"Inactive"}</span></div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:12,marginTop:18,fontSize:12}}><div><span className="muted">Department</span><br/><strong>{d.departments.find(y=>y.id===x.departmentId)?.name}</strong></div><div><span className="muted">Consultation fee</span><br/><strong>PKR {x.fee.toLocaleString()}</strong></div><div><span className="muted">Duration</span><br/><strong>{x.duration} minutes</strong></div><div><span className="muted">Utilization</span><br/><strong>{ap.length?Math.round(completed/ap.length*100):0}%</strong></div></div><div style={{marginTop:15,padding:11,borderRadius:8,background:"#f8fafc",fontSize:12,display:"flex",gap:8}}><Clock3 size={15}/>{x.hours.start}–{x.hours.end} · Break {x.break.start}–{x.break.end}</div></article>})}</div></div>}
