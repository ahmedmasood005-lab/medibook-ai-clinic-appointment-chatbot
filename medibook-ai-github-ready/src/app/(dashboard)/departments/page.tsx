import {Building2} from "lucide-react";import {readStore} from "@/lib/store";import {PageHeader} from "@/components/page";export default async function Departments(){const d=await readStore();return <div className="page-pad" style={{padding:26}}><PageHeader title="Departments" description="Clinic specialties, service duration, active doctors, and demand."/><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",gap:14}}>{d.departments.map(x=><article className="card" key={x.id} style={{padding:18}}><div style={{display:"flex",justifyContent:"space-between"}}><span style={{display:"grid",placeItems:"center",width:40,height:40,borderRadius:9,background:"#ecfeff",color:"#0f766e"}}><Building2 size={20}/></span><span className={`badge status-${x.active?"Confirmed":"Cancelled"}`}>{x.active?"Active":"Inactive"}</span></div><h2 style={{fontSize:17,margin:"16px 0 7px"}}>{x.name}</h2><p className="muted" style={{fontSize:13,lineHeight:1.6,minHeight:42}}>{x.description}</p><div style={{display:"flex",gap:18,fontSize:12,borderTop:"1px solid var(--line)",paddingTop:13}}><span><strong>{d.doctors.filter(y=>y.departmentId===x.id&&y.active).length}</strong> doctors</span><span><strong>{x.duration}</strong> min</span><span><strong>{d.appointments.filter(y=>y.departmentId===x.id).length}</strong> appointments</span></div></article>)}</div></div>}
