import {PageHeader} from "@/components/page";export default function About(){const items=[['Project ID','Mtech-DS26004'],['Student','Ahmed Masood'],['Project title','Custom AI Chatbot with Tool-Use (Function Calling) for Clinic Appointments'],['Category','LLM / Tool Use / Function Calling']];return <div className="page-pad" style={{padding:26,maxWidth:1000}}><PageHeader eyebrow="Academic project" title="About MediBook AI" description="A production-style clinic appointment administration platform with grounded AI tool-use."/><div className="card" style={{padding:24}}><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:14}}>{items.map(([k,v])=><div key={k} style={{padding:14,borderRadius:8,background:"#f8fafc"}}><small className="muted">{k}</small><div style={{fontWeight:800,marginTop:6,lineHeight:1.45}}>{v}</div></div>)}</div>{[['Project overview','MediBook AI unifies traditional clinic operations with an AI assistant that executes authenticated server-side tools.'],['Main objectives','Reliable doctor discovery, availability checking, patient registration, appointment lifecycle management, auditability, and role-based security.'],['Technology stack','Next.js App Router, strict TypeScript, Tailwind CSS, Prisma-compatible data models, OpenAI function calling, Zod, Recharts, React Hook Form, and secure signed sessions.'],['Architecture summary','Server components query clinic data; route handlers validate and authorize mutations; the AI loop calls the same appointment services as the dashboard.'],['Ethical AI statement','The assistant supports administration only. It does not diagnose, prescribe, interpret reports, or replace medical professionals.'],['Limitations','Demonstration data is fictional. Revenue is estimated. Notifications and payment processing are not connected to external providers.']].map(([h,p])=><section key={h} style={{marginTop:25,borderTop:"1px solid var(--line)",paddingTop:18}}><h2 style={{fontSize:16}}>{h}</h2><p className="muted" style={{fontSize:14,lineHeight:1.7}}>{p}</p></section>)}</div></div>}
