import {readStore} from "@/lib/store";import {PageHeader} from "@/components/page";import {saveSettingsAction} from "@/app/actions";export default async function Settings(){const d=await readStore(),s=d.settings;return <div className="page-pad" style={{padding:26,maxWidth:1000}}><PageHeader title="Settings" description="Clinic hours, scheduling rules, notifications, privacy, and assistant configuration."/><form action={saveSettingsAction} className="card" style={{padding:22,display:"grid",gap:20}}><section><h2 style={{fontSize:16}}>Clinic profile</h2><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:12}}><label className="label">Clinic name<input name="clinicName" className="field" defaultValue={String(s.clinicName)}/></label><label className="label">Default timezone<input name="timezone" className="field" defaultValue={String(s.timezone)}/></label></div></section><section style={{borderTop:"1px solid var(--line)"}}><h2 style={{fontSize:16}}>Scheduling</h2><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(160px,1fr))",gap:12}}><label className="label">Opening time<input name="openingTime" className="field" type="time" defaultValue={String(s.openingTime)}/></label><label className="label">Closing time<input name="closingTime" className="field" type="time" defaultValue={String(s.closingTime)}/></label><label className="label">Duration (minutes)<input name="duration" className="field" type="number" defaultValue={Number(s.duration)}/></label><label className="label">Buffer (minutes)<input name="buffer" className="field" type="number" defaultValue={Number(s.buffer)}/></label></div></section><label className="label">Cancellation policy<textarea name="cancellationPolicy" className="field" defaultValue={String(s.cancellationPolicy)}/></label><section style={{borderTop:"1px solid var(--line)"}}><h2 style={{fontSize:16}}>Privacy and AI</h2><p className="muted" style={{fontSize:13,lineHeight:1.6}}>The assistant is restricted to appointment administration and must not diagnose, prescribe, interpret reports, or provide emergency advice. Prompts and audit logs should exclude sensitive clinical details.</p><label style={{display:"flex",gap:9,fontSize:13}}><input name="aiEnabled" value="yes" type="checkbox" defaultChecked={Boolean(s.aiEnabled)}/> Enable appointment assistant</label><label style={{display:"flex",gap:9,fontSize:13,marginTop:10}}><input name="notifications" value="yes" type="checkbox" defaultChecked={Boolean(s.notifications)}/> Enable operational notifications</label></section><button className="btn btn-primary" style={{justifySelf:"start"}}>Save settings</button></form></div>}
